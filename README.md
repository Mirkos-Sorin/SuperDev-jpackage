# SuperDev-jpackage
This program can very conveniently help you use that annoying jpackage. With this, you can quickly use it through a GUI interface, instead of writing commands longer than the Great Wall in the command line.
